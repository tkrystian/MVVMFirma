<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieOpinieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id Opinii" Binding="{Binding IdOpinii}"/>
                <DataGridTextColumn Header="Id Restauracji" Binding="{Binding IdRestauracji}"/>
                <DataGridTextColumn Header="Id Klienta" Binding="{Binding IdKlienta}"/>
                <DataGridTextColumn Header="Ocena" Binding="{Binding Ocena}"/>
                <DataGridTextColumn Header="Komentarz" Binding="{Binding Komentarz}"/>
                <DataGridTextColumn Header="Data" Binding="{Binding Data}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
